name: Security audit
description: Runs cargo audit via the RustSec advisory database

runs:
  using: composite
  steps:
    - uses: rustsec/audit-check@v2
      with:
        token: ${{ github.token }}
